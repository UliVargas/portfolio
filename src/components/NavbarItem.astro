---
interface Props {
  href: string
  label: string
}

const { href, label } = Astro.props

const pathname = new URL(Astro.request.url);
console.log(pathname);

//const currentPath = pathname.slice(1); // remove the first "/"
---

<li>
  <a class={`px-6 py-4 rounded hover:bg-red-600`} href={href}>{label}</a>
</li>